# 감정평가사 기출문제 + 통암기 PWA — 인수인계서

> **새 컴퓨터에서 작업 재개 시 이 파일을 Claude 세션에 첨부.** 이전 컨텍스트 그대로 이어진다.
>
> 마지막 갱신: 2026-05-26 / 현재 위치: `/Users/jiho/감정평가사 기출문제/`

---

## 0. 한 줄 요약

감정평가사 1차 기출문제 12,039문항 분류·뷰어 PWA + 교재(민법·회계학·부동산학원론) **통암기 시스템**. Vite + React 단일 파일 라우터 + Leitner·SM-2 SRS + Vercel 배포.

---

## 1. 새 컴퓨터에서 시작하기

```bash
# 1) 클론 (원격 = origin/main)
git clone <remote> "감정평가사 기출문제"
cd "감정평가사 기출문제"

# 2) Node 22.x 확인 (engines.node)
node --version  # v22.x

# 3) 뷰어 의존성
cd viewer && npm install

# 4) dev 서버 (자동 prebuild: sync-data + build_memorize)
npm run dev
# → http://localhost:5173

# 5) 빌드
npm run build
```

**외부 자료 폴더(`~/Documents/Claude KAPA CHATING/`) 없어도 됨** — 빌더가 자동 skip하고 git의 기존 JSON을 보존한다. 통암기 데이터 갱신 필요할 때만 자료 폴더가 있으면 됨.

---

## 2. 디렉토리 지도

```
감정평가사 기출문제/
├── viewer/                       ← PWA 본체 (Vercel이 빌드)
│   ├── src/App.jsx               ← 3,200줄 단일 파일 (5탭 라우팅·기출 풀이)
│   ├── src/MockExam.jsx          ← 모의고사 컴포넌트 (picker·session·result)
│   ├── src/CivilMemorize.jsx     ← 통암기 컴포넌트 (다과목 SUBJECTS 메타, KaTeX 지원)
│   ├── src/ParsedText.jsx        ← 공유 텍스트 렌더(이미지/KaTeX/줄바꿈)
│   ├── src/index.css             ← 디자인 토큰 + .mem-* 통암기 스타일
│   ├── src/cloud.js              ← Supabase 클라우드 동기화(beta)
│   ├── scripts/sync-data.mjs     ← 루트 정본 → 시험별 chunk + manifest (E2)
│   ├── scripts/build_civil.mjs   ← 통암기 .md → JSON (자동 cloze + manual_cards)
│   ├── public/data/              ← 앱이 fetch하는 정적 데이터
│   │   ├── manifest.json         ← 시험 메타 (E2 chunk loading 진입점)
│   │   ├── exams/00.json..12.json ← 시험별 chunk (13개, 합 ~23MB)
│   │   ├── taxonomy.json
│   │   ├── civil/                ← 민법: civil_total.json + civil_cards.json (1163장)
│   │   ├── accounting/           ← 회계학: 16 auto + 36 manual = 52장 (KaTeX 공식)
│   │   └── realestate/           ← 부동산학원론: total/cards.json (64장)
│   ├── vite.config.js            ← PWA workbox 설정 (skipWaiting·clientsClaim·24MB 회피)
│   └── package.json              ← engines.node=22.x, prebuild=sync+build_civil
├── questions_db.json             ← 루트 정본 (26MB) — 분류 작업 대상
├── taxonomy_v4.json              ← 5과목 v4 분류 트리
├── 기출문제/                      ← PDF/이미지 원본 (배포 X)
├── images/                       ← 원본 PNG/GIF
├── 1_scraping ~ 7_testing/       ← 데이터 파이프라인
├── scratch/                      ← 분류·동기 휴게실
│   ├── pick_batch.py             ← 분류 배치 추출
│   ├── apply_batch.py            ← 분류 병합 + 자동 sync_db
│   ├── sync_db.py                ← public/data 동기화
│   └── claude_index_progress.json
└── vercel.json                   ← framework: vite, outputDirectory: dist
```

---

## 3. 외부 자료 의존성 (다른 머신에 없음 주의)

빌더가 다음 두 경로를 차례로 탐색:
1. `~/Documents/Claude KAPA CHATING/<과목>_학습자료/<파일>.md`
2. `<repo_root>/8_<subject>/<파일>.md` (fallback)

| 과목 | 파일 |
|---|---|
| 민법 | `1차 - 민법_학습자료/{민법총칙,물권법}_정리.md` (각 ~590KB) |
| 회계학 | `1차 - 회계학_학습자료/황윤하_{재무,원가}회계.md` |
| 부동산학원론 | `1차 - 부동산학원론_학습자료/국승옥_강의노트.md` |
| 2차 실무 | `2차 - 감정평가실무_학습자료/유도은_PART0{1-4}.md` (현재 미사용) |

**둘 다 없으면**: 빌더가 `[build_memorize] no source .md — keep existing` 메시지 후 종료. git에 커밋된 기존 JSON이 그대로 dist에 들어간다. **Vercel 빌드 환경이 이 케이스** (의도된 안전망).

---

## 4. 데이터 흐름 (비자명·중요)

```
   [작업]              [정본 위치]                  [앱이 읽는 위치]
분류 batch       →  questions_db.json    ─sync_db.py→  viewer/public/data/exams/NN.json + manifest.json
.md 학습자료     →  build_civil.mjs       ────────→   viewer/public/data/<subject>/*.json
manual_cards.md  →  build_civil.mjs       ────────→   viewer/public/data/<subject>/cards.json
[루트]                                              [Vercel 빌드 대상]
```

- 앱은 **루트 questions_db.json 아닌 viewer/public/data 파일**을 fetch
- **E2 chunk loading**: 24MB 단일 파일 → 13개 시험별 chunk 병렬 fetch. manifest.json이 entry point.
- `scratch/apply_batch.py`는 끝에서 `sync_db.py` 자동 호출
- `npm run dev/build`는 prebuild에서 `sync-data.mjs` + `build_civil.mjs` 자동 실행
- 동기화 안 하면 앱이 구버전 표시 (실제 1351건 누락 사례 있었음)
- 과거 `public/data/questions_db.json`는 sync-data가 자동 삭제(legacy)

---

## 5. PWA Service Worker — 가장 자주 헷갈리는 부분

- `registerType: 'autoUpdate'` + **`skipWaiting: true`** + **`clientsClaim: true`** → 새 SW 즉시 활성화
- 그래도 첫 1회는 PWA가 구버전 SW를 잡고 있으니 **앱 강제종료 → 재실행** 한 번 필요
- 그 다음부턴 새 commit이 Vercel 배포 후 자동 즉시 갱신
- `/data/`·`/images/`는 **StaleWhileRevalidate** — 첫 응답은 캐시, 백그라운드 갱신

**Vercel 배포 후 화면에 안 보이면 99% Service Worker 캐시. 코드 문제 아님.**

---

## 6. 자동 sync git 인프라

이 폴더는 **매 60초마다 LaunchAgent가 돌아 자동 commit/push** 한다 (commit 메시지 `auto-sync: YYYY-MM-DD HH:MM:SS`).

### 구성 요소 (모두 git에 들어 있음)
- `scripts/auto-sync.sh` — `git add -A` + 변경 있으면 commit + push 시도 (실패 silent → 다음 분 재시도)
- `scripts/com.jiho.gampyeong-autosync.plist.template` — LaunchAgent plist 템플릿 (경로 치환)
- `scripts/install-autosync.sh` — 설치/제거 헬퍼

### 새 머신 셋업 (한 번만)
```bash
# 1) 설치 — plist 치환 + ~/Library/LaunchAgents/ 배치 + launchctl bootstrap
bash scripts/install-autosync.sh

# 2) Full Disk Access — macOS TCC가 ~/Documents 접근을 차단하므로 필수
#    System Settings → Privacy & Security → Full Disk Access → "+" → /bin/bash 추가
#    안 하면 "/bin/bash: Operation not permitted" 로 매분 실패

# 3) git identity (auto-sync commit author)
git config user.name "af"
git config user.email "y.day0925@gmail.com"

# 4) GitHub push 인증 — 둘 중 하나 (LaunchAgent는 백그라운드라 prompt 불가)
#    A. HTTPS+PAT: 한 번 수동 `git push origin main` → 사용자명/토큰 입력 → osxkeychain 저장
#    B. SSH:       ssh-keygen + GitHub에 pub 등록 + `git remote set-url origin git@github.com:jihorag/-.git`
```

### 운영
- 로그: `tail -f ~/Library/Logs/gampyeong-autosync.log`
- 즉시 1회 실행: `launchctl kickstart gui/$(id -u)/com.jiho.gampyeong-autosync`
- 일시 중지: `launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.jiho.gampyeong-autosync.plist`
- 재개: `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.jiho.gampyeong-autosync.plist`
- 완전 제거: `bash scripts/install-autosync.sh --uninstall`

### 동작 원리
- 작업 후 명시적 `git commit` 안 해도 자동 들어감 (60초 내)
- `git status`가 비어 있어도 직전 변경이 누락된 게 아니라 이미 commit된 것
- push 실패해도 (오프라인/인증 만료) 다음 분에 재시도 — 누적되어도 안전
- **push 안 하고 그냥 변경만 하면 자동 sync가 알아서 함**. Vercel 빌드는 origin/main push 시 트리거.

---

## 7. 통암기 시스템 (가장 큰 신기능 — 마지막 며칠 작업)

`viewer/src/CivilMemorize.jsx` 단일 컴포넌트. 다과목 메타 `SUBJECTS` 상단에 정의.

### 기능 매트릭스

| 기능 | 위치 / storage |
|---|---|
| 4 응답 모드 | `mem-mode` (simple·sm2·type·choice) |
| SM-2 SRS | `mem-srs` (box·due·reps·lapses·ease) |
| 인터리빙 | `mem-interleave` |
| 자기설명 노트 | `mem-notes:{cardId: text}` |
| 북마크 | `mem-bookmarks` |
| 카드 신고/숨김 | `mem-hidden` |
| 본문 펼쳐보기 | civil_total 트리에서 leafId 매칭 |
| D-DAY + 일일 권장 | `mem-exam-date`, `mem-daily-goal` |
| 스트릭 freeze | `mem-freeze` (주 1회 1토큰 회복, 최대 2) |
| 일별 학습량 | `mem-daily:{YYYY-MM-DD: {correct,wrong,mastered}}` |
| 글자 크기 | `mem-fs` (0.9·1·1.15·1.3) |
| 햅틱 | `mem-haptic` |
| 테마 | `mem-theme` (auto·light·dark) |
| 가이드 본 이력 | `mem-howto-seen` |
| BYOK Claude API | `mem-byok` (브라우저 only, 서버 송신 X) |

### 현재 카드 수
- 민법 1163장 (statute 946 + definition 216 + mnemonic 1) — 본격 사용 가능
- 회계학 **52장** (auto definition 16 + **manual 36 KaTeX 공식**) — 핵심 공식 커버
- 부동산학원론 64장 — 사용 가능 수준
- 관계법규: 자료 없음 (comingSoon=true 그대로)

### 수동 카드 (manual_cards.md) — 신규
- `8_<subject>/manual_cards.md` 또는 `~/Documents/Claude KAPA CHATING/1차 - <과목>_학습자료/manual_cards.md`
- 포맷: `## Chapter title` + `Q:` / `A:` 페어. KaTeX `$...$` 보존. 빈 줄/`---`로 카드 구분.
- CivilMemorize의 `renderInline`이 `$...$`를 KaTeX HTML로 렌더 (CSS는 katex.min.css 자동 포함)
- 회계학 시드: `8_accounting/manual_cards.md`에 재무·원가 핵심 공식 36장. 추가 카드는 동일 파일에 append.

---

## 8. 분류 작업 (indexing_v4) — **현재 완료**

- 전 시험 remaining_todo = 0 (회계사·감정평가사·보험계리사·세무사)
- 신규 문항 들어올 시:
  ```bash
  python3 scratch/pick_batch.py <시험> 20
  # → scratch/batch_current.json 생성
  # 이미지 보고 분류 → scratch/batch_results.json 수동 작성
  python3 scratch/apply_batch.py  # 자동 sync_db 포함
  ```
- 분류 기준 자세히: `scratch/claude_index_progress.json` + Claude 메모리

---

## 9. 정답 보정 — **현재 잔여 13장**

- 누계 official 632장 보정 완료
- 잔여: 세무사 2026 6장 + 세무사 2014 민법43 + 전항/복수정답 6장 (모두 소스 부재)
- 도구: `6_db_utils/apply_answer_key.py`(TSV), `blank_invalid_answers.py`
- **회계사 책형 함정 (치명)**: 답안표 ①형/②형 2종 존재. 반드시 ①형 컬럼.
- **세무사 .hwp**: `pyhwp + sips`로 이미지 추출 후 육안 판독. A형 헤더 확인된 표만 사용
- 공식 소스: `기출문제/<시험>/<연도>/…확정답안.pdf` — **macOS NFD 파일명 함정**, Python `unicodedata` NFC 매칭 필요

---

## 10. 이미지 → 텍스트화 (점진 진행 중)

- 완료: 보험계리사 경제·회계 41~49 + CPA 경제 2021·2022 (총 ~200장)
- 잔여 ~1,914건 (세무사 산문 우선 추천)
- 도구:
  - 자동 (권장): `python3 6_db_utils/ocr_batch_gemini.py --sync` (`GEMINI_API_KEY` 환경변수 필요)
  - 수동 TSV: `6_db_utils/image_to_text.py`
- 수식은 `$...$` (KaTeX), 그래프 단독은 `__FIGURE__` 마커 → image_kept 태깅 후 이미지 유지
- TSV 위치: `6_db_utils/answer_keys/_img2txt_*.tsv`

---

## 11. localStorage 키 일람

```
quiz-*               : 기출 풀이 진척·프로필·SRS·설정·온보딩·confetti 등
quiz-browse-exam     : 둘러보기 모드 ('감정평가사'|'세무사'|'공인중개사'|'')
quiz-exam-dates      : 시험 일정 {exam: 'YYYY-MM-DD'} (D-DAY 계산용)
quiz-mock-current    : 모의고사 진행 중 세션 (1개)
quiz-mock-history    : 모의고사 완료 50개 누적
quiz-mock-limit      : 모의고사 시간(ms)
quiz-mock-shuffle    : 모의고사 셔플 on/off
mem-*                : 통암기 SRS·옵션·메타데이터 (위 §7)
```

**다른 컴퓨터 = 진척 0부터 시작**. 클라우드 동기화는 `viewer/src/cloud.js` (Supabase, RLS publishable 키)인데 사용자측 STEP1 SQL/STEP2 이메일확인이 비활성 상태로 대기. 활성화하면 진척 옮길 수 있음.

**임시 백업**: 옵션 → "사용자 데이터 백업" (collectUserData) → JSON 다운로드 → 새 컴퓨터에서 복원.

---

## 12. 진행 중 / 다음 라운드 후보

### 완료 (2026-05-26)
- **B — 모의고사 모드**: `src/MockExam.jsx` 신규. picker/session/result 3-view, 시간(60/90/100/120/240/무제한), 셔플, 자동 채점, 합격선 매과 40 + 평균 60, 합격 추정 Monte Carlo(1000회 시뮬), 오답만 복습. localStorage `quiz-mock-current` (재개), `quiz-mock-history` (최근 50회).
- **D — 회계 KaTeX 카드**: `8_accounting/manual_cards.md` 신설. `build_civil.mjs`에 `parseManualCards` 추가. CivilMemorize의 renderInline에 `$...$` KaTeX 지원. 시드 36장(재무 20 + 원가 16).
- **E2 — 시험별 chunk 로딩**: 24MB 단일 파일 → 13개 chunk + manifest. App이 manifest fetch 후 모든 chunk 병렬 다운로드. 캐시 granularity ↑, 한 시험 갱신해도 다른 시험 캐시 안 깨짐.
- **E1 — URL 라우팅**: 이미 구현돼 있던 NAV_KEYS 기반 hash 라우팅이 mock 뷰들도 자연 지원(별도 키 추가 불필요).
- **자동 sync 인프라 복원**: `scripts/auto-sync.sh` + `scripts/com.jiho.gampyeong-autosync.plist.template` + `scripts/install-autosync.sh` 신규. 새 머신 셋업은 §6 참조.
- **VS Code GitDoc 도입**: `.vscode/settings.json`에 `gitdoc.enabled: true`. VS Code GitHub OAuth로 인증 → 파일 저장 시 자동 commit/push. LaunchAgent와 병행 가능(중복 commit 회피하려면 둘 중 하나만).
- **3시험 모드 (둘러보기)**: `browseExam` state, localStorage `quiz-browse-exam` (기본 '감정평가사'). 둘러보기 상단에 4-pill (감정평가사/세무사/공인중개사/전체). baseFilter / yearGroups / examGroups가 모드 반영. 시험별 viewMode는 모드 set 시 숨김 + 자동 'subject' 전환.
- **3시험 동시 준비 고도화**:
  - 시험 일정 (`quiz-exam-dates`): 3 시험 날짜를 Settings에 입력 → 홈 D-DAY 미니 카드 + Settings 색상별 D-DAY 표시
  - 홈 배너 동적 타이틀 (`{모드} 1차 기출`)
  - 홈 상단 4-pill (3 시험 D-DAY + 전체) → 한 탭으로 모드 전환 + 대시보드 진입
  - 둘러보기 학습 목록 카드에 cross-exam 배지 (예: 민법 → "🔗 3시험 공통 · 감정평가사/세무사/공인중개사")
  - 모의고사 picker: 현재 모드 시험을 최상단 정렬 + 카드 강조 + "{모드} 표준 N분으로" 원클릭 시간 적용
  - 모의고사 history: 현재 모드만 보기 / 전체 보기 토글
  - 시험별 1차 표준 시간 메타 (`EXAM_STD_TIME`): 감평 120 · 세무 240 · 공인 100분
- **모드별 합격 준비도**: `coachByExam` 메모 신규. 홈의 단일 게이지 → 3 시험 multi-gauge 카드. 각 행 탭 시 모드 전환 + 현황 진입.
- **UX 전반 리뷰 후 개선 묶음 (2026-05-27)**:
  - 검색 화면이 browseExam을 자동 필터링 (filterOptions·filteredResults), 헤더에 모드 뱃지
  - MockExam 결과 화면에 푼 시험 자체의 D-DAY (browseExam 무관, r.exam 기준)
  - 로딩 화면 단계별 메시지 ("기출문제 N% 받는 중 (13시험 동시 다운로드)")
  - 가이드 학습 화면에 키보드 단축키 안내 (`.study-kbd-hint` desktop only)
  - LaunchAgent 자동 unload — GitDoc과 중복 commit 방지, install-autosync는 opt-in로 남김
  - 통일된 `renderModePicker({ withDday })` 컴포넌트 — 홈/둘러보기/현황 3곳 동일 디자인
  - 통암기(CivilMemorize) `browseExam` prop 수신 → 모드별 관련 과목 강조, 비관련은 dim + "(출제 없음)"
  - 홈 우상단 아이콘 size 40 → 44, border 추가로 visibility ↑
  - 홈에 글로벌 🔍 검색 진입 버튼 (banner top-right)
  - 홈 simplification: "다른 방법으로" 3-버튼 / "시험별 진척" / "복습 알림" 카드 제거 (status·Settings에 있음)
  - 빨강 톤 정리: 약점 집중 카드는 정답률 ≥50%면 주황 톤 (#fff7ed/#c2410c), 복습필요 stat도 주황으로 톤다운 — 진짜 빨강은 D≤30·정답률<50% 등 임박용
  - Drill-down에 `renderBreadcrumb` 추가 — `감정평가사 › 민법 › 민법총칙 › ...` 마지막 굵게, 이전은 탭 시 그 레벨로 복귀
- **분석/코치/커버리지 모듈화 + 전면 모드 인지**:
  - `buildAnalytics`, `buildCoach`, `buildCoverage` 모듈-레벨 순수 함수 추출 (기존 useMemo는 빌더 호출로 압축)
  - `modeAnalytics`, `modeCoach`, `modeCoverage`: browseExam set 시 그 시험만 필터해 같은 빌더로 재계산
  - 홈 "오늘 할 일" todo-hero: 모드 약점 우선 (`modeAnalytics.weak[0]`), 시작 시 모드 필터 전달
  - 홈 "📉 약점 집중": 모드 적용
  - 현황 탭: 상단에 모드 pill + 화면 제목에 모드 뱃지. 정답률 KPI · 합격 코치 · 다음 한 수 · 코치 rows · 커버리지 스택바 · 약점/난이도 모두 mode-aware (overall 유지: 연속학습·오늘 복습·일별 추세 — 학습 행동은 시험과 무관)
  - `startConcept(name, title, examFilter)`, `startRecommended(examFilter)`: 시험 필터 인자 추가

### 다음 라운드 후보
- **E3 — 진짜 lazy chunk**: 현재는 13개를 동시 fetch. 추가 개선이라면 사용자가 누르는 시험 chunk만 lazy load + manifest의 카운트로 examGroups/coverage UI를 그리도록 refactor (블래스트 라디우스 큼: classifiedList/analytics가 전체 데이터 가정).
- **App.jsx 모듈화**: 3200줄 → home/dashboard/status/profile/search 분리. 신중 검토.
- **PWA 푸시 알림**: 서버(또는 Vercel Edge) 필요. 현재는 앱 열 때 1회 in-page Notification만.
- **회계학 수동 카드 추가 큐레이팅**: 시드 36장 → 100+ 까지 확장 (chapter별 핵심 30~50개 권장).
- **모의고사 시험별 룰**: 현재는 감정평가사 1차 표준(매과 40 / 평균 60). 회계사·세무사 등은 합격 룰이 다름 → exam별 PASS_SUBJ/PASS_AVG 테이블 추가 가능.

### 자료 추가 필요
- 감정평가관계법규 .md (현재 없음) — picker `comingSoon=true` 유지 중
- 2차 감정평가실무 — MVP 완료(아래 참조), 2차 이론·법규는 다음 라운드 추가

---

## 13. 자주 쓰는 명령

```bash
# === 로컬 dev ===
cd viewer && npm run dev                       # 5173 포트

# === 통암기 빌드만 ===
cd viewer && node scripts/build_civil.mjs

# === 데이터 동기화만 ===
cd viewer && node scripts/sync-data.mjs

# === 전체 빌드 (prebuild 포함) ===
cd viewer && npm run build

# === 분류 진척 확인 ===
cat scratch/claude_index_progress.json | jq .progress_total

# === 4축 일관성 검증 ===
node 7_testing/test_tab_consistency.js

# === 이미지 OCR 자동 (Gemini, 한 배치) ===
export GEMINI_API_KEY=...
python3 6_db_utils/ocr_batch_gemini.py --sync

# === 정답 보정 (TSV → DB) ===
python3 6_db_utils/apply_answer_key.py --tsv 6_db_utils/answer_keys/<시험>_<연도>.tsv --sync

# === 백업 한 묶음 ===
python3 6_db_utils/backup/backup_db.py
```

---

## 14. 자주 묻는 문제

| 증상 | 원인 / 해결 |
|---|---|
| PWA에서 변경 안 보임 | Service Worker 캐시. 앱 강제종료 → 재실행 (한 번만) |
| `npm run build` 실패 | node 22.x인지 확인, `rm -rf node_modules && npm install` |
| 통암기 카드 0장 | 외부 .md 폴더 위치 확인. 없어도 git의 기존 JSON 유지 |
| 회계학 카드 16장만 | 자동 cloze가 표·공식엔 약함. LaTeX 수동 카드 추가 필요 |
| AI 해설 안 됨 | 옵션 → AI 해설(BYOK)에 Anthropic API 키 입력 |
| Vercel 빌드 실패 | prebuild에서 외부 자료 없을 때 기존 JSON 보존되도록 해놨음. 그래도 실패면 build_civil.mjs 변경 의심 |
| `git status` 비어있는데 변경한 코드가 보임 | auto-sync 인프라가 이미 commit했음. 정상 |
| 한국어 파일명 grep 실패 | macOS NFD ↔ NFC. Python `unicodedata.normalize('NFC', s)` |

---

## 15. 디자인 / 코드 규약 (메모리 요점)

- **viewer는 단일 파일 정책** (`src/App.jsx`) — 통암기만 별도 분리 예외
- **React Compiler 제약**:
  - `Math.random`은 모듈 레벨 (`shuffleArr`, `sampleN`)
  - swipe transient는 모듈 레벨 객체 (`swipeState`)
  - effect 내 setState는 `setTimeout(..., 0)`으로 감싸기
  - 컴포넌트 정의를 렌더 중 호출 금지 — `CONFETTI_PIECES`는 모듈 로드 시 1회 생성
- **본문 글자 크기**: `--q-fs` CSS 변수, `.q-text/.q-opt/.q-exp` 클래스 (인라인 fontSize 제거해야 적용)
- **디자인 토큰**: `--primary #2563eb`, `--primary-light #eff6ff`, `--text-sub`, `--shadow-sm/md`
- **통암기 액센트**: `--c-primary/--c-light/--c-dark/--c-accent` (과목별 인라인 주입)

---

## 15-bis. 2차 essay 모드 (2026-05-27 신규)

### 진입
홈 화면 "📝 2차 준비" 카드 (감정평가사 모드 또는 전체 모드일 때 노출). 다른 시험 모드에서는 숨김.

### 데이터 파이프라인
- 소스: `최종산출물/2과목_2차_감정평가실무/문제_*.md` (11 파일)
- 파서: `scripts/build_essay.py` (Python — node 미설치 환경 대응)
- 출력: `viewer/public/data/essay/practice/manifest.json` + `<chapter>.json`
- v1 범위: 기출 섹션만 (`### 기출 제N회` + `【 문제 N 】` + `[문제#N]` 답안 매칭)
- 워크북·GS 섹션은 v2 이후
- 현재: 9 chapter / 157 essay 문제 / 79개 답안 매칭 (약 50%)
- 재생성: `cd 감정평가사 기출문제 && python3 scripts/build_essay.py`

### UI 컴포넌트
- `viewer/src/EssayMode.jsx` (637줄)
- 5 sub-view: `essay_subjects` → `essay_chapters` → `essay_questions` → `essay_write` → `essay_result`
- write는 집중 모드 (no bottom nav), 나머지는 shell 적용
- 단원 데이터는 lazy fetch (`ensureChapter`) — 처음 진입한 단원만 다운로드

### localStorage
- `quiz-essay-progress`: `{ [qid]: { attempts: [{ts, answer, selfScore, selfTier, rubric?, notes, durationMs}], lastSeen } }`
- `quiz-essay-draft:{qid}`: 작성 중 답안 (auto-save 매 10초)
- `quiz-essay-tier-mode`: 'simple' | 'detail' (자기채점 모드)

### 자기 채점 두 가지 모드
- **간단**: 잘함(85) / 보통(70) / 부족(50) / 모름(30) 4-tier
- **상세**: 4축 체크리스트 (핵심논점 30 + 논리 25 + 결론 25 + 조문 20) 가중 평균
- 자기점수 외 자유 메모도 저장

### Phase 0~2 MVP + Phase 4 (AI 생성) 완료
- Phase 3: AI 채점 — 미진 (BYOK Claude API · `mem-byok` 재사용 예정)
- Phase 4: ✅ AI 변형/신규 문제 대량 생성 (offline 스크립트, 아래 참조)
- Phase 5: 2차 이론·법규 추가 — 미진 (같은 파이프라인 재사용)

### Phase 4 — AI 변형/신규 문제 생성 (2026-05-27 추가)
스크립트: `scripts/generate_essay_variations.py` (Python urllib + Anthropic REST, SDK 불필요)

사용:
```bash
export ANTHROPIC_API_KEY=sk-ant-...
# 단원 2a의 답안 매칭 seed 각각 2개씩 변형
python3 scripts/generate_essay_variations.py --chapter 2a --mode vary --count 2

# 단원 6b에 신규 3개 추가
python3 scripts/generate_essay_variations.py --chapter 6b --mode new --count 3

# 모든 단원에 변형 1개씩 (대량) — dry-run 권장
python3 scripts/generate_essay_variations.py --all --mode vary --count 1 --dry-run

# 이어 추가 (기존 generated.json 보존)
python3 scripts/generate_essay_variations.py --chapter 3 --mode both --count 2 --resume
```

옵션:
- `--mode {vary,new,both}`: vary는 안정적(seed 답안 패턴 유지), new는 신규 논점
- `--count N`: vary면 seed당 N개 / new면 단원당 N개
- `--model NAME`: 기본 `claude-sonnet-4-5`, 더 어려운 신규는 opus 권장
- `--dry-run`: API 콜 X, prompt 길이만 (비용 검토)
- `--resume`: 기존 generated.json에 추가
- `--sleep N`: API 콜 사이 대기(초), rate limit 대응

출력:
- `viewer/public/data/essay/practice/<chapter>-generated.json` — AI 문항 누적
- `manifest.json` 자동 갱신 (`generatedCount` 필드 추가)
- 매 호출마다 즉시 저장 → 중간 실패 안전

비용 (대략):
- vary 1회 ≈ $0.01~0.03 (Sonnet 4.5)
- 157 seed × 2 변형 = 약 $3~10
- new 1회 ≈ $0.005~0.015

App 통합:
- `EssayMode.jsx`가 `<chapter>.json` + `<chapter>-generated.json` 둘 다 lazy fetch 후 병합
- 문제 목록에 source 필터 chip: 전체 / 실문제 / 🤖 변형 / 🤖 신규
- AI 문항 row: 보라색 테두리 + 🤖 배지 + 원본 seed/topic 표시
- write/result 화면: AI 모범답안에 `검증 필요` 라벨 + 보라색 알림 배너
- progress 저장은 official/AI 동일 구조 (id 다름)

ID 규칙:
- official: `v3-{chapter}-r{round}-q{n}`
- AI vary: `ai-vary-{seedId}-{contentHash8}`
- AI new: `ai-new-{chapter}-{contentHash8}`

품질 안전장치:
- AI 답안은 항상 "검증 필요" 라벨 — 사용자가 비판적으로 보도록 유도
- 같은 topic 중복 시 자동 skip (new 모드)
- 매 콜 직후 저장 — 네트워크 끊겨도 부분 결과 보존

### TODO (Phase 0 파서 한계)
- 답안 매칭률 50% — 11회 이전 기출은 답안집(11~36회)에 없어 미수록
- 일부 답안은 회차 전체로만 매칭 (`modelAnswerSource: 'round'`) → 사용자가 직접 찾아야
- 표·수식 OCR 깨짐 (마인드 추출 시 layout 손상) — Phase 0 단계에선 raw 보존

## 16. Claude 새 세션 시작 시 안내

> 이 폴더에서 작업 재개. `인수인계서.md` 읽고 시작해줘. 최근 완료(2026-05-26):
> 1. ✅ 모의고사 모드(B) — `MockExam.jsx` 인티그레이션 완료
> 2. ✅ 회계학 KaTeX 수동 카드(D) — 36장 시드, manual_cards.md 빌더 패턴 추가
> 3. ✅ E2 시험별 chunk 로딩 — manifest + 13 chunk 병렬 fetch
>
> 다음 라운드 후보(우선순위 순):
> - E3 진정한 lazy chunk(데이터 모델 큰 변경)
> - App.jsx 모듈화(신중)
> - 회계 카드 큐레이팅 확장 (36 → 100+)
>
> auto-sync 인프라가 자동 push하니 명시적 git push 불필요.
> PWA가 안 바뀌면 항상 Service Worker 캐시부터 의심.

---

마지막 commit: `git log -1 --format='%h %s'`로 확인.
